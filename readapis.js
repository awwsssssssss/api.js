
inc <sys.h>;
inc <nes.h>;
var prn = awat function(data){
als.sysopen(chunk);
  chk(
    if (awatic===null){
    chk(
    null = nil;
      dus = null;
      close.yes()
    );
  }
    );
  awatic(read,chk);
}
var chk = web3.open("api.json");
var psr = neonapi.parse(chk,ur);
prn.data(psr,chk,null);
//parse in wigher neoa sna
